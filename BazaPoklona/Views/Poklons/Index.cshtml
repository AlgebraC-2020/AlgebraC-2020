@model IEnumerable<BazaPoklona.Models.Poklon>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kupljen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VrstaRobeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kupljen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VrstaRobeNavigation.Naziv)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPoklon">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPoklon">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPoklon">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
